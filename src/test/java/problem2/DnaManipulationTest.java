package problem2;

import org.junit.jupiter.api.Test;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Paths;

import static org.junit.jupiter.api.Assertions.*;

class DnaManipulationTest {

    /*@Test
    void longestCommonSubShouldTestCase1() {
        assertEquals(3, DnaManipulation.longestCommonSub("AGC", "ATGCA"));
    }*/

    /*@Test
    void longestCommonSubShouldTestCase2() {
        assertEquals(6, DnaManipulation.longestCommonSub("AACCTTGG", "ACACTGTGA"));
    }*/

    /*@Test
    void longestCommonSubShouldTestCase3() throws IOException {
        BufferedReader br = Files.newBufferedReader(Paths.get("src", "main", "resources", "problem2a.txt"));
        String x = br.readLine().replace("\n", "").replace("\r", "").replace("\r\n", "");
        String y = br.readLine().replace("\n", "").replace("\r", "").replace("\r\n", "");
        String exp = br.readLine().replace("\n", "").replace("\r", "").replace("\r\n", "");
        assertEquals(exp.length(), DnaManipulation.longestCommonSub(x, y));
    }*/

    /*@Test
    void optimalAlignmentCostShouldTestCase1() {
        assertEquals(-4, DnaManipulation.optimalAlignmentCost("AGC", "ATGCA"));
    }*/

    /*@Test
    void optimalAlignmentCostShouldTestCase2() {
        assertEquals(-8, DnaManipulation.optimalAlignmentCost("AACCTTGG", "ACACTGTGA"));
    }*/

    /*@Test
    void optimalAlignmentCostShouldTestCase3() throws IOException {
        BufferedReader br = Files.newBufferedReader(Paths.get("src", "main", "resources", "problem2a.txt"));
        String x = br.readLine().replace("\n", "").replace("\r", "").replace("\r\n", "");
        String y = br.readLine().replace("\n", "").replace("\r", "").replace("\r\n", "");
        assertEquals(-604, DnaManipulation.optimalAlignmentCost(x, y));
    }*/
}